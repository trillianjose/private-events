<div class="col-md-8">
  <h3>Events (<%= @user.count %>)</h3>
  <ol class="microposts">
    <% @user.each do |event| %>
      <h3><%= event.title %></h3>
      <p><%= event.description %></p>
    <% end %>
  </ol>
</div>
